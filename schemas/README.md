Project schemas

- Store project-specific schema files under `schemas/<project>/`.
- Current default project: `glioblastoma_perplexity_manual/` (empty placeholder until schema processing is implemented).
