Project notebooks

- Place analysis notebooks under `notebooks/<project>/` to keep outputs and inputs scoped per project.
- Default project: `glioblastoma_perplexity_manual/` (directory created). Update notebook paths to point at `data/<project>/` outputs.
