[project]
name = "glioblastoma-perplexity-ds-manual"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "jupyter",
    "pandas",
    "matplotlib",
    "numpy",
    "openai>=1.0.0",
    "python-dotenv",
]
